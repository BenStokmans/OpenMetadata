{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DucklakeConnection",
  "description": "DuckLake is an open Lakehouse format that is built on SQL and Parquet",
  "type": "object",
  "javaType": "org.openmetadata.schema.services.connections.database.DucklakeConnection",
  "definitions": {
    "ducklakeType": {
      "description": "Service type.",
      "type": "string",
      "enum": [
        "Ducklake"
      ],
      "default": "Ducklake"
    },
    "ducklakeScheme": {
      "description": "SQLAlchemy driver scheme options.",
      "type": "string",
      "enum": [
        "duckdb"
      ],
      "default": "duckdb"
    }
  },
  "properties": {
    "type": {
      "title": "Service Type",
      "description": "Service Type",
      "default": "Ducklake",
      "type": "string",
      "enum": [
        "Ducklake"
      ]
    },
    "scheme": {
      "title": "Connection Scheme",
      "description": "SQLAlchemy driver scheme options.",
      "default": "duckdb",
      "type": "string",
      "enum": [
        "duckdb"
      ]
    },
    "metadataPath": {
      "title": "Metadata Path",
      "description": "DuckLake metadata catalog path or connection string. Examples: metadata.ducklake, postgres:dbname=postgres, or a DuckDB secret name.",
      "type": "string"
    },
    "dataPath": {
      "title": "Data Path",
      "description": "Optional DuckLake data storage location. Examples: s3://bucket/path/ or /path/to/files/.",
      "type": "string"
    },
    "catalogName": {
      "title": "Catalog Name",
      "description": "Catalog name used to attach DuckLake in DuckDB.",
      "type": "string",
      "default": "ducklake"
    },
    "databaseName": {
      "title": "Database Name",
      "description": "Optional name to give to the database in OpenMetadata. If left blank, the attached catalog name is used.",
      "type": "string"
    },
    "databaseSchema": {
      "title": "Database Schema",
      "description": "Database Schema of the data source. This is optional parameter, if you would like to restrict the metadata reading to a single schema.",
      "type": "string"
    },
    "readOnly": {
      "title": "Read Only",
      "description": "Attach DuckLake in read-only mode.",
      "type": "boolean",
      "default": true
    },
    "createIfNotExists": {
      "title": "Create If Not Exists",
      "description": "Create a new DuckLake if the configured metadata catalog does not exist.",
      "type": "boolean",
      "default": false
    },
    "overrideDataPath": {
      "title": "Override Data Path",
      "description": "Override the data path stored in DuckLake metadata for this connection.",
      "type": "boolean",
      "default": false
    },
    "awsConfig": {
      "title": "S3 Credentials Configuration",
      "description": "Optional credentials used by DuckDB to read DuckLake catalogs or data paths in S3-compatible object storage. Leave empty for public HTTPS catalogs.",
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "javaType": "org.openmetadata.schema.services.connections.database.DucklakeS3Credentials",
      "properties": {
        "enabled": {
          "title": "Enable IAM Auth",
          "description": "Use the default AWS credential provider chain.",
          "type": "boolean",
          "default": false
        },
        "awsAccessKeyId": {
          "title": "AWS Access Key ID",
          "description": "AWS or S3-compatible access key ID.",
          "type": "string"
        },
        "awsSecretAccessKey": {
          "title": "AWS Secret Access Key",
          "description": "AWS or S3-compatible secret access key.",
          "type": "string",
          "format": "password"
        },
        "awsRegion": {
          "title": "AWS Region",
          "description": "Optional region for AWS or S3-compatible object storage.",
          "type": "string"
        },
        "awsSessionToken": {
          "title": "AWS Session Token",
          "description": "Optional session token for temporary AWS credentials.",
          "type": "string"
        },
        "endPointURL": {
          "title": "Endpoint URL",
          "description": "Optional endpoint URL for S3-compatible object storage.",
          "type": "string",
          "format": "uri"
        },
        "profileName": {
          "title": "Profile Name",
          "description": "Optional AWS profile used by the credential provider chain.",
          "type": "string"
        },
        "assumeRoleArn": {
          "title": "Role Arn for Assume Role",
          "description": "Optional Amazon Resource Name of a role to assume.",
          "type": "string"
        },
        "assumeRoleSessionName": {
          "title": "Role Session Name for Assume Role",
          "description": "Optional identifier for the assumed role session.",
          "type": "string",
          "default": "OpenMetadataSession"
        },
        "assumeRoleSourceIdentity": {
          "title": "Source Identity for Assume Role",
          "description": "Optional source identity for the assumed role session.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "connectionOptions": {
      "title": "Connection Options",
      "javaType": "org.openmetadata.schema.services.connections.database.ConnectionOptions",
      "description": "Additional connection options to build the URL that can be sent to service during the connection.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "connectionArguments": {
      "title": "Connection Arguments",
      "javaType": "org.openmetadata.schema.services.connections.database.ConnectionArguments",
      "description": "Additional connection arguments such as security or protocol configs that can be sent to service during connection.",
      "type": "object",
      "additionalProperties": {
        ".{1,}": {
          "type": "string"
        }
      }
    },
    "schemaFilterPattern": {
      "title": "Default Schema Filter Pattern",
      "description": "Regex to only include/exclude schemas that matches the pattern.",
      "type": "object",
      "javaType": "org.openmetadata.schema.metadataIngestion.FilterPattern",
      "properties": {
        "includes": {
          "description": "List of strings/regex patterns to match and include only database entities that match.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "excludes": {
          "description": "List of strings/regex patterns to match and exclude only database entities that match.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "tableFilterPattern": {
      "title": "Default Table Filter Pattern",
      "description": "Regex to only include/exclude tables that matches the pattern.",
      "type": "object",
      "javaType": "org.openmetadata.schema.metadataIngestion.FilterPattern",
      "properties": {
        "includes": {
          "description": "List of strings/regex patterns to match and include only database entities that match.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "excludes": {
          "description": "List of strings/regex patterns to match and exclude only database entities that match.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "databaseFilterPattern": {
      "title": "Default Database Filter Pattern",
      "description": "Regex to only include/exclude databases that matches the pattern.",
      "type": "object",
      "javaType": "org.openmetadata.schema.metadataIngestion.FilterPattern",
      "properties": {
        "includes": {
          "description": "List of strings/regex patterns to match and include only database entities that match.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "excludes": {
          "description": "List of strings/regex patterns to match and exclude only database entities that match.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "supportsMetadataExtraction": {
      "title": "Supports Metadata Extraction",
      "description": "Supports Metadata Extraction.",
      "type": "boolean",
      "default": true
    },
    "supportsDBTExtraction": {
      "description": "Supports DBT Extraction.",
      "type": "boolean",
      "default": true
    }
  },
  "additionalProperties": false,
  "required": [
    "metadataPath"
  ]
}